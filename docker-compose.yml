services:
  server:
    build:
      context: ./server
    container_name: loyalty_tournament_server
    working_dir: /app
    command: sh -c "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - ./server/.env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg2://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-finalrep}}
      CORS_ALLOWED_ORIGINS: http://localhost:5174,http://localhost:3000
      LEADERBOARD_BASE_URL: http://localhost:5174/
    volumes:
      - ./server:/app
    ports:
      - "8001:8000"

  client:
    build:
      context: ./client
    container_name: loyalty_tournament_client
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    environment:
      VITE_PROXY_TARGET: http://server:8000
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ./client:/app
      - loyalty_tournament_client_node_modules:/app/node_modules
    ports:
      - "5174:5173"
    depends_on:
      - server

volumes:
  loyalty_tournament_client_node_modules:
